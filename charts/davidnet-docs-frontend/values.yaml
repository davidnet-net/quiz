replicaCount: 1

image:
  repository: ghcr.io/davidnet-net/quiz
  pullPolicy: Always
  tag: "latest"

service:
  type: ClusterIP
  port: 3000

ingress:
  enabled: true
  host: quiz.davidnet.net
  ingressClass: traefik
